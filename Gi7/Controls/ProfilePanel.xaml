<UserControl x:Class="Gi7.Controls.ProfilePanel" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}">
    <ScrollViewer Foreground="White">
        <Grid Background="#000000">
            <Grid.RowDefinitions>
                <RowDefinition Height="240" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border BorderBrush="Gainsboro" Padding="5" Background="White" HorizontalAlignment="Center"
                    VerticalAlignment="Center" Width="240" Height="240">
                <Image
                    Source="{Binding User.AvatarUrl, ConverterParameter=210, Converter={StaticResource GravatarSizeConverter}}" />
            </Border>
            <TextBlock Grid.Row="1" Text="{Binding User.Name}" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Margin="0,0,5,0" Foreground="{StaticResource PhoneAccentBrush}" FontSize="40" />
            <TextBlock Grid.Row="2" Text="{Binding User.Login}" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Margin="0,0,5,0" Foreground="{StaticResource PhoneAccentBrush}" />
            <Rectangle Grid.Row="3" Fill="Gainsboro" Height="1" />
            <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="5" Visibility="{Binding User.Company, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <Image Source="/Images/company.png" Width="16" Margin="10,0" />
                <TextBlock Text="{Binding User.Company}" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="5" Margin="5" Visibility="{Binding User.Location, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <Image Source="/Images/location.png" Width="16" Margin="10,0" />
                <TextBlock Text="{Binding User.Location}" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="6" Margin="5" Visibility="{Binding User.Email, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <Image Source="/Images/mail.png" Width="16" Margin="5,0" />
                <HyperlinkButton Margin="0" Content="{Binding User.Email}" NavigateUri="{Binding User.Email}" TargetName="_blank"
                                 HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="7" Margin="5" Visibility="{Binding User.Blog, Converter={StaticResource EmptyStringToVisibilityConverter}}">
                <Image Source="/Images/link.png" Width="16" Margin="5,0" />
                <HyperlinkButton Margin="0" Content="{Binding User.Blog}" NavigateUri="{Binding User.Blog}" TargetName="_blank"
                                 HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="8" Margin="5">
                <Image Source="/Images/time.png" Width="16" Margin="10,0" />
                <TextBlock Text="{Binding User.Joined, StringFormat=MMM dd\, yyyy}" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
            <Rectangle Grid.Row="9" Fill="Gainsboro" Height="1" />
            <Grid Grid.Row="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="{Binding User.Followers}" HorizontalAlignment="Center" FontSize="40"/>
                    <TextBlock Text="followers" HorizontalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding User.PublicRepos}" HorizontalAlignment="Center" FontSize="40"/>
                    <TextBlock Text="public repos"  />
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding User.Following}" HorizontalAlignment="Center" FontSize="40"/>
                    <TextBlock Text="following" />
                </StackPanel>
            </Grid>
            <HyperlinkButton Content="See on github" NavigateUri="{Binding User.HtmlUrl}" Grid.Row="11"
                             TargetName="_blank" HorizontalAlignment="Right" />
        </Grid>
    </ScrollViewer>
</UserControl>