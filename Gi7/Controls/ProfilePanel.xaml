<UserControl x:Class="Gi7.Controls.ProfilePanel" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
             mc:Ignorable="d" FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}" Foreground="{StaticResource PhoneForegroundBrush}" d:DesignHeight="480"
             d:DesignWidth="380">


    <ListBox x:Name="ProfileListBox" Margin="12,0,12,0">
        <ListBoxItem>
            <StackPanel Margin="10,0,0,7" Width="432" Height="150">
                <Image Source="{Binding Path=User.AvatarUrl, Converter={StaticResource GravatarSizeConverter}, ConverterParameter=150}"
                        VerticalAlignment="Top" HorizontalAlignment="Left" Width="150" Height="150"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Login" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.Login}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem Visibility="{Binding User.Name, Converter={StaticResource EmptyConverter}}">
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Name" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.Name}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem Visibility="{Binding User.Location, Converter={StaticResource EmptyConverter}}">
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Location" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.Location}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem Visibility="{Binding User.Blog, Converter={StaticResource EmptyConverter}}">
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Web" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <HyperlinkButton Content="Blog" NavigateUri="{Binding User.Blog}" TargetName="_blank" />
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Public repos" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.PublicRepos}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Private repos" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.TotalPrivateRepos}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Followers" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.Followers}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                <TextBlock Text="Following" TextWrapping="Wrap" Style="{StaticResource PhoneTextExtraLargeStyle}"/>
                <TextBlock Text="{Binding User.Following}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" Foreground="{StaticResource PhoneAccentBrush}"/>
            </StackPanel>
        </ListBoxItem>
        <ListBoxItem>
            <StackPanel Margin="0,0,0,7" Width="432" Height="78">
                 <HyperlinkButton Content="See on github" NavigateUri="{Binding User.HtmlUrl}" TargetName="_blank"
                                  HorizontalAlignment="Left" FontSize="35" />
            </StackPanel>
        </ListBoxItem>
    </ListBox>
</UserControl>