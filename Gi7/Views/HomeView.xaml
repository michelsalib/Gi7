<phone:PhoneApplicationPage 
    x:Class="Gi7.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:Interactivity="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:Command="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP71"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="clr-namespace:Gi7.Utils"
    xmlns:localControls="clr-namespace:Gi7.Controls"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="728"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    DataContext="{Binding Path=HomeViewModel, Source={StaticResource ViewModelLocator}}">

    <Grid x:Name="LayoutRoot">
        <controls:Panorama x:Name="Panorama">
            <Interactivity:Interaction.Triggers>
                <Interactivity:EventTrigger EventName="SelectionChanged">
                    <Command:EventToCommand Command="{Binding PanoramaChangedCommand}" PassEventArgsToCommand="True" />
                </Interactivity:EventTrigger>
            </Interactivity:Interaction.Triggers>
            
            <controls:Panorama.Title>
                    <TextBlock Text="Gi7" FontSize="100" Margin="0,50,0,0" />
            </controls:Panorama.Title>

            <controls:PanoramaItem Header="Login" Visibility="{Binding IsLoggedOut, Converter={StaticResource BoolToVisibilityConverter}}">
                <localControls:LoginPanel/>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="News Feed" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                <localControls:FeedsPanel/>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="Repos" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                <localControls:RepositoryPanel/>
            </controls:PanoramaItem>
            
            <controls:PanoramaItem Header="Users" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                <localControls:UsersPanel/>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="Profile" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVisibilityConverter}}">
                <localControls:UserPanel/>
            </controls:PanoramaItem>

            <controls:PanoramaItem Header="About">
                <localControls:AboutPanel/>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" Mode="Minimized">
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="Logout" Click="Logout"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>
</phone:PhoneApplicationPage>